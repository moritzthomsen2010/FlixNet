spring:
  application:
    name: "FlixNet"

  datasource:
    url: jdbc:h2:mem:flixnet
    driverClassName: org.h2.Driver
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
